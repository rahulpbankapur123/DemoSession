name: my first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome Message
          run: echo "My first GitHub Actions Job"
        
        - name: List files
          run: ls -lrt

        - name: Read the File
          run: cat README.md
        
